# Validation Report — Prefab

**Date:** 2026-05-18
**Branch:** `v2.2.x/tool-consolidation`
**Validator:** build-validator agent
**Plan validated:** `.claude/reports/plans/plan-Prefab-20260518.md`
**Files in scope:** 11 (6 modified `.cs`, 3 created `.cs`, 2 modified prompt `.cs`) + 1 stub-or-deleted (`Tool_AddAssetToScene.cs`)
**Status:** READY FOR COMMIT (PASS with 1 informational note)

---

## 0. Summary

- Files validated: 11 source files inspected + 1 expected-stub file confirmed absent (not breaking)
- Static convention checks: PASS (0 errors, 0 warnings)
- `dotnet build`: SKIPPED — no `.csproj` exists in the package (expected for a Unity package; Unity generates csproj only when the package is opened inside a project)
- `tsc --noEmit`: SKIPPED — no `Server~/` source changes in scope (per invocation)
- Overall: READY FOR COMMIT

---

## 1. Static Convention Findings

### Errors
None.

### Warnings
None.

### Spot checks performed

| Check | Result |
|-------|--------|
| Braces on every `if` | PASS — no single-line `if (...) statement;` bodies. The two candidates flagged by the regex (`ModifyContents.cs:141`, `ModifyContents.cs:435`) are Allman-style with the `{` on the next line. |
| Empty `catch` blocks | PASS — both `try`/`catch` blocks in `Override.cs` (`apply-instance`, `revert-instance`) log via `Debug.LogWarning` before returning a `ToolResponse.Error`. No empty handlers anywhere. |
| `McpLogger.Warning` not used | PASS — zero matches across the entire `Editor/` tree. `Override.cs` uses `Debug.LogWarning` as required. |
| `InstanceIDToObject` not used directly | PASS — zero matches in `Editor/Tools/Prefab/`. All four lookup sites (`Create.cs:37`, `CreateVariant.cs:34`, `UnpackInstance.cs:33`, `Override.cs:41`) route through `Tool_Transform.FindGameObject(int, string)`, which still wraps the deprecated API behind a `#pragma warning disable CS0618` until the Transform audit migrates it. This is the documented intentional dependency. |
| Null-conditional assignment (`?.x = y`) | PASS — zero matches. |
| `[McpTool]` ID format (kebab-case) | PASS — all 10 IDs lowercase kebab: `prefab-close`, `prefab-create`, `prefab-create-variant`, `prefab-get-info`, `prefab-instantiate`, `prefab-modify-contents`, `prefab-open`, `prefab-override`, `prefab-save`, `prefab-unpack-instance`. |
| `[Description]` on every `[McpTool]` method | PASS — confirmed on Close, Create, CreateVariant, GetInfo, Instantiate, ModifyContents, Open, Override, Save, UnpackInstance. All come from `[System.ComponentModel.Description]` on the method declaration (not from the `Description` property of the `[McpTool]` attribute). |
| `[Description]` on every parameter of `[McpTool]` methods | PASS — every parameter has its own `[Description]` attribute. |
| XML `<param>` / `<returns>` coverage on tool methods | PASS — every public `[McpTool]` method has full XML doc: `<summary>`, one `<param>` per parameter, `<returns>` describing the `ToolResponse`. |
| Brace balance per file | PASS — all 10 Prefab partial files balance their `{` / `}` counts (verified via regex match counts). Prompt files (`Prompt_GameObjectHandling.cs`, `Prompt_PrefabWorkflow.cs`) also balance. |

---

## 2. `dotnet build` Result

**Status:** SKIPPED — no `.csproj` found in the repository.

`Glob` for `**/*.csproj` returned zero matches. This is expected for a Unity Editor package: csproj files are generated by Unity into the *host project's* `Library/` (or `obj/`) when the package is opened, never inside the package source itself.

**Caveat for Ramon:** the C# compiler was not invoked by this validator. The convention checks above are stylistic + pattern-based, not semantic. If you want a hard compile signal, the only reliable way is to let Unity open the host project and report compile errors in the Console. Given the static checks are clean, I expect Unity to compile this cycle without errors, but I cannot prove it.

---

## 3. `tsc --noEmit` Result

**Status:** SKIPPED — invocation explicitly states the consolidator did NOT touch `Server~/` source. No TypeScript files in scope. Markdown files under `Server~/prompts/` and `Plugin~/` were also explicitly out of scope.

---

## 4. Plan-Specific Spot Checks

### 4.1 Sentinel convention enforcement

| Field | File | Spec | Actual | Verdict |
|-------|------|------|--------|---------|
| `isActive` (set-active action) | `ModifyContents.cs:57` | `string`, `"true"\|"false"\|""` sentinel, error when `""` | `[Description("...")] string isActive = ""` — handler at lines 295–324 errors when `norm` is empty, parses `"true"`/`"false"`, errors on any other literal | PASS |
| `action` (Override) | `Override.cs:29` | `string`, no default, empty returns error listing valid values | `[Description("...")] string action = ""` — line 36–39 returns error: *"'action' is required. Valid values: 'list', 'apply-instance', 'revert-instance'."* | PASS |
| `action` (ModifyContents) | `ModifyContents.cs:51` | `string`, no default, empty returns error listing valid values | `[Description("...")] string action = ""` — line 111–115 returns error listing all six valid actions | PASS |
| `unpackMode` | `UnpackInstance.cs:28` | `string`, `"outermost"\|"completely"`, NOT enum/int | `[Description("...")] string unpackMode = "outermost"` — handler at lines 45–63 maps to `PrefabUnpackMode` enum internally, returns clear error on unknown value | PASS |

No `int` tri-state for booleans, no `enum` parameters on tool surfaces. The legacy `int 1/0/-1` pattern is fully replaced in this domain. Internal value-bag fields in `ModifyContents` (`fieldValueInt = int.MinValue`, `fieldValueFloat = float.NegativeInfinity`) remain numeric — these are correct per the plan because they carry actual numeric payloads, not booleans.

### 4.2 Single class-level `[McpToolType]` `<summary>`

Searched `Editor/Tools/Prefab/` for `[McpToolType]` — exactly one match at `Tool_Prefab.Close.cs:15`. The preceding `<summary>` (lines 10–14) describes the entire partial class. Verified that the three new partial files do NOT carry a class-level `<summary>`:

- `CreateVariant.cs:11` → `public partial class Tool_Prefab` preceded only by `namespace` + `{`. Clean.
- `UnpackInstance.cs:11` → same. Clean.
- `Override.cs:12` → same. Clean.

The existing siblings (`Open.cs`, `Save.cs`, `Create.cs`, `GetInfo.cs`, `Instantiate.cs`, `ModifyContents.cs`) also correctly have only method-level XML doc on the `partial class` declaration. No duplication.

### 4.3 FUTURE CONSOLIDATION pointer in `ModifyContents.cs`

`Tool_Prefab.ModifyContents.cs:228–232` carries:

```
// TODO(v2.1.x): align this implementation with the canonical pattern resolved by the
// post-v2.0 backlog's "Object <-> ScriptableObject <-> Component generic-modifier triangle"
// (see docs/internal/post-v2.0-backlog.md -> Component consolidation cycle). When the
// triangle is resolved, extract a shared helper or refactor this block to match the
// canonical SerializedObject-based field-setter.
```

This satisfies the plan's "FUTURE CONSOLIDATION" comment requirement: it explicitly references the post-v2.0 backlog Object/SO/Component triangle as the v2.1.x revisit target, and points at the canonical doc. The literal phrase "FUTURE CONSOLIDATION" is not used; the comment uses `TODO(v2.1.x)` instead, which is a clearer convention. Intent and content match the plan. **PASS.**

### 4.4 `using UnityEditor.SceneManagement;` in `Override.cs`

The consolidator's heads-up was: *"I used `var o = overrides[i]` to let type inference handle `ObjectOverride` without an explicit `using`."*

Verified state:
- `Override.cs:1–8` imports: `System.ComponentModel`, `System.Text`, `GameDeck.MCP.*`, `UnityEditor`, `UnityEngine`. No `using UnityEditor.SceneManagement;`.
- Line 66: `var overrides = PrefabUtility.GetObjectOverrides(go, false);` — return type is `List<ObjectOverride>` where `ObjectOverride` lives in `UnityEditor.SceneManagement`.
- Line 74: `var o = overrides[i];` — type inferred, never written literally.

**Expected verdict:** compiles. `var` infers the element type from the indexer's return type; the `ObjectOverride` symbol does not need to be in source scope because it's never named there. The assembly reference (`UnityEditor.SceneManagementModule`) is provided transitively by Unity's Editor asmdef.

**Caveat (cannot confirm without `dotnet build`):** if Unity changes the namespace placement of `ObjectOverride` or the asmdef gets trimmed, this would surface as `CS0246: The type or namespace name 'ObjectOverride' could not be found` when accessing members. As a defensive measure, adding `using UnityEditor.SceneManagement;` to `Override.cs` would harden the file with no downside — but it is not required to compile today. **Optional polish, not a blocker.** PASS.

### 4.5 Stubbed `Tool_AddAssetToScene.cs`

**Finding:** the file does not exist at `Editor/Tools/AddAssetToScene/Tool_AddAssetToScene.cs`. The entire `AddAssetToScene` folder is absent.

**Analysis:**
- `Grep` for `Tool_AddAssetToScene` across all source files returns zero matches (only audit/plan/review docs reference it).
- `Grep` for `AddAssetToScene` across all source returns zero source matches (same: only docs).
- No caller in `Editor/Tools/`, `Editor/Prompts/`, `Server~/`, etc. references the deleted tool.

**Verdict:** PASS. Either Ramon already deleted the stub via VS Code, or the consolidator skipped creating it and proceeded directly to deletion-by-omission. Either way, the absence is not a compile risk because nothing depends on it. The plan's "minimal shell with TODO" was a transitional safety net; since nothing references the legacy tool, the safety net is unneeded. **No action required.**

### 4.6 Cross-domain prompt substitutions

`Editor/Prompts/Prompt_GameObjectHandling.cs` and `Editor/Prompts/Prompt_PrefabWorkflow.cs` were skimmed:
- `Prompt_GameObjectHandling.cs:35` now reads `- prefab-instantiate — instantiate prefabs or model assets into the scene` — kebab tool ID present.
- `Prompt_PrefabWorkflow.cs:39` reads `2. Instantiate into scene with prefab-instantiate` — kebab tool ID present.
- `Prompt_PrefabWorkflow.cs:57` reads `- prefab-instantiate — instantiate prefabs or model assets` — kebab tool ID present.

Two stale legacy mentions remain in `Prompt_PrefabWorkflow.cs`:
- Line 34: `6. Save as prefab using create_prefab tool` — should be `prefab-create`.
- Line 44: `4. Apply changes back to prefab (apply_prefab_overrides)` — should be `prefab-override` with `action='apply-instance'`.

These are NOT flagged as blockers because they live in a *prompt body* (a string literal for an LLM-facing workflow doc), not in code that the C# compiler analyzes. The MCP server will happily ship a prompt that mentions tool names which no longer exist; the LLM will discover the actual tool surface via `tools/list`. However, **as a content-correctness note, these two lines should be updated in a follow-up commit** (or by Ramon in this same VS Code session) to keep the prompt internally consistent with the post-consolidation tool surface. Logging here as INFO, not WARNING, because they were outside the explicit Group C substitution targets in the plan (lines 39/45/57 only).

---

## 5. Overall Verdict

**READY FOR COMMIT.**

- All hard convention checks pass.
- All plan-specific spot checks pass (sentinel correctness, single class-summary, FUTURE CONSOLIDATION pointer, kebab-case IDs, full XML docs, no forbidden APIs).
- The `using UnityEditor.SceneManagement;` question in `Override.cs` is benign — `var` inference handles it. Add the using only if you want defensive hardening; it is not required to compile.
- `Tool_AddAssetToScene.cs` does not exist; no caller references it; no compile risk.
- `dotnet build` could not be invoked (no csproj — Unity package, as expected). Unity's compile pass on next domain reload is the authoritative confirmation.

Recommendations for Ramon at commit time:
1. (Optional) Add `using UnityEditor.SceneManagement;` to `Override.cs` as defensive hardening — no behavior change.
2. (Optional, separate commit) Update two stale prompt-body lines in `Prompt_PrefabWorkflow.cs` (line 34 `create_prefab` → `prefab-create`; line 44 `apply_prefab_overrides` → `prefab-override action='apply-instance'`).
3. Open Unity on the host project and confirm clean compile + that the 10 prefab tools register in the MCP tool list before tagging the cycle complete.

---

## 6. Validator Caveats

- **`dotnet build` was skipped** because no csproj is reachable from the package root. Unity packages do not ship csproj. The authoritative compile signal must come from Unity opening the host project. Convention checks here are necessary but not sufficient.
- **Convention checks are heuristic.** The regex passes for "single-line `if`" and "null-conditional assignment" are tight but not formal C# AST analysis; a Roslyn analyzer would be stronger. Nothing in this cycle triggered any flags, so the heuristic is adequate here.
- **No git was touched** (per Rule 1). File discovery was entirely from the invocation's explicit list, the plan, and `Glob`/`Grep` against the package tree.
- **No source files were modified** (per Rule 3). Only this report file was written.
