# Validation Report — Asset

**Date:** 2026-05-19
**Validator:** build-validator agent
**Plan validated:** `.claude/reports/plans/plan-Asset-20260519.md`
**Status:** PASSED

---

## 0. Summary

- Files validated: 12 (5 created, 7 modified, +1 confirmed-deleted)
- Convention checks: clean — 0 errors, 0 warnings in scope
- `dotnet build`: skipped — no `.csproj` reachable from the package root (Unity package; csproj is generated by Unity in the host project, which is outside this repo)
- `tsc --noEmit`: skipped — no `Server~/` changes in this cycle (confirmed)
- Overall: **PASSED**

---

## 1. Convention Checks

Each file in scope was checked against the CLAUDE.md C# standards:

1. XML doc summary + `<param>` + `<returns>` on every public method
2. Braces on every `if` (no single-line `if (x) return ...;`)
3. No empty `catch` blocks
4. No `McpLogger.Warning` (does not exist)
5. No `InstanceIDToObject` (use `EntityIdToObject`)
6. No null-conditional assignment (`obj?.prop = ...`)
7. `[McpTool]` methods have `[System.ComponentModel.Description]`
8. Every parameter on `[McpTool]` methods has `[Description]`
9. `[McpTool]` IDs are lowercase kebab-case
10. Brace count balanced per file
11. `[McpToolType]` + class-level XML summary on exactly one Asset partial
12. `using System.ComponentModel;` present where `[Description]` is used

### Errors
None.

### Warnings
None.

### Per-file summary

| File | XML doc | Braces | catch | Logger | DeprAPI | Null-assign | [McpTool] desc | Param desc | Tool-ID | Brace bal |
|------|---------|--------|-------|--------|---------|-------------|----------------|-----------|---------|-----------|
| `Tool_Asset.Shared.cs` (new) | ok | ok | n/a | ok | ok | ok | n/a (no `[McpTool]`) | n/a | n/a | 25/25 |
| `Tool_Asset.CreateRenderTexture.cs` (new) | ok | ok | n/a | ok | ok | ok | ok | ok (6/6) | `asset-create-render-texture` ok | 23/23 |
| `Tool_Asset.SetLabels.cs` (new) | ok | ok | n/a | ok | ok | ok | ok | ok (3/3) | `asset-set-labels` ok | 31/31 |
| `Tool_Asset.FindReferences.cs` (new) | ok | ok | n/a | ok | ok | ok | ok | ok (2/2) | `asset-find-references` ok | 23/23 |
| `Tool_Asset.Exists.cs` (new) | ok | ok | n/a | ok | ok | ok | ok | ok (1/1) | `asset-exists` ok | 13/13 |
| `Tool_Asset.Find.cs` (mod) | ok | ok | n/a | ok | ok | ok | ok | ok (3/3) | `asset-find` ok | 10/10 |
| `Tool_Asset.ImportSettings.cs` (mod) | ok | ok | n/a | ok | ok | ok | ok (both) | ok (1/1 + 2/2) | `asset-get-import-settings`, `asset-set-import-settings` ok | 52/52 |
| `Tool_Asset.Refresh.cs` (mod) | ok | ok | n/a | ok | ok | ok | ok | ok (1/1) | `asset-refresh` ok | 6/6 |
| `Tool_Asset.CreateFolder.cs` (mod) | ok | ok | n/a | ok | ok | ok | ok | ok (1/1) | `asset-create-folder` ok | 11/11 |
| `Tool_Asset.Rename.cs` (mod) | ok | ok | n/a | ok | ok | ok | ok | ok (2/2) | `asset-rename` ok | 13/13 |
| `Tool_Asset.Delete.cs` (mod) | ok | ok | n/a | ok | ok | ok | ok | ok (2/2) | `asset-delete` ok | 10/10 |
| `Tool_Asset.Copy.cs` (mod) | ok (incl. `[McpToolType]` summary) | ok | n/a | ok | ok | ok | ok | ok (2/2) | `asset-copy` ok | 12/12 |

### Special-attention items (per invocation)

**A. `Tool_Asset.Create.cs` empty / pending delete.** The consolidator report said "file still on disk at size 0". On inspection, the file does **not** exist on disk — neither `Tool_Asset.Create.cs` nor `Tool_Asset.Create.cs.meta` are present in `Editor/Tools/Asset/`. This is the desired end-state and removes any risk of a stray empty file producing a compile/import error. The consolidator appears to have actually deleted it rather than leaving it at 0 bytes; report wording lagged the action. No follow-up needed.

**B. Nullability of `Tool_Asset.SetLabels.cs`.** `#nullable enable` is set at line 1. `ParseStringArrayJson` is declared `private static string[]? ParseStringArrayJson(string json)` and assigned to `string[]? incoming = ParseStringArrayJson(labelsJson);` followed by an explicit `if (incoming == null)` guard before any indexing. Nullable contract is clean.

**C. Signature alignment Shared.cs ↔ ImportSettings.cs.**
- Declared in `Tool_Asset.Shared.cs:21`: `private static bool ApplyStringValueToProperty(SerializedProperty prop, string value)`
- Called in `Tool_Asset.ImportSettings.cs:169`: `bool applied = ApplyStringValueToProperty(prop, rawValue);` — `prop` is `SerializedProperty`, `rawValue` is `string`. ✅
- Declared in `Tool_Asset.Shared.cs:119`: `private static string[] SplitJsonEntries(string jsonBody)`
- Called in `Tool_Asset.ImportSettings.cs:130`: `string[] entries = SplitJsonEntries(inner);` — `inner` is `string`. ✅
- Both helpers are in the same partial class `Tool_Asset` in namespace `GameDeck.Editor.Tools`. Compiler will resolve them as same-class members.

**D. `[McpToolType]` placement.** Confirmed present on exactly one file (`Tool_Asset.Copy.cs:17`), with the class-level XML `<summary>` block directly above it. No other Asset partial has either `[McpToolType]` or a class-level XML doc — per CLAUDE.md "Partial classes: XML doc summary goes on EXACTLY ONE file". ✅

---

## 2. `dotnet build` Output

**Command:** *(not run)*
**Status:** skipped — no `.csproj` found

### Reason

`Glob` for `**/*.csproj` from the repo root returned zero matches. This is expected for a Unity Editor package: csproj/sln files are generated by Unity into the *host project's* root (e.g. `JurassicSurvivors/Assembly-CSharp-Editor.csproj`), not into the package. The host project is outside this repository.

`dotnet --version` succeeded (10.0.300), so the SDK is available — there is just nothing here for it to build standalone.

To get a compiled-build signal on this consolidation, open the Jurassic Survivors test project in Unity 6000.x with this package referenced; Unity will regenerate the csproj and report compile errors in the Console. The static checks above are the substitute available in this environment.

---

## 3. `tsc --noEmit` Output

**Command:** *(not run)*
**Status:** skipped — no `.ts` files in scope

### Reason

The consolidator report lists only `.cs` files in `Editor/Tools/Asset/`. No `Server~/` paths were modified in this cycle. Running `tsc` would not exercise any changed code, so it is intentionally skipped (not a validator gap).

---

## 4. Recommendations

- **Ready for VS Code review and commit.** Static convention checks are clean across all 12 files in scope. The signature contract between `Shared.cs` and `ImportSettings.cs` is correct. The deleted-file end-state (`Tool_Asset.Create.cs` gone, no stray `.meta`) is what we want.
- **Compile-time confirmation requires Unity.** Recommend Ramon opens the test project once after pulling this branch so Unity regenerates csproj and reports any compile errors in the Console. Static checks cannot substitute for an actual `csc` pass on subtle issues (e.g. missing `using`, ambiguous overloads, accessibility mismatches across partials) — but no such risk was *visible* in the source-level inspection.
- **No re-run of the consolidator is required.**

---

## 5. Validator Caveats

- **`dotnet build` was not executed** because no csproj is reachable from the package root. This is structural to Unity packages, not a validator failure. The static convention checks in §1 are precise (regex + read inspection) but cannot detect *semantic* compile errors — missing `using` directives, type mismatches, accessibility conflicts, etc. Confidence is high based on inspection, but `csc` is authoritative for compilation.
- **The convention checks were heuristic Grep patterns plus targeted Reads** of all 12 in-scope files. I read each file in scope start-to-end at least once.
- **Out-of-scope finding (not actionable for this validation):** `Editor/Tools/Asset/Tool_Asset.GetInfo.cs:67` has `if (i > 0) sb.Append(", ");` — a single-line `if` without braces, violating CLAUDE.md. This file was **not** in the consolidator's scope for this cycle (it was not listed as Created or Modified), so it is not the consolidator's responsibility. Recording here as an observation so Ramon can decide whether to fix opportunistically or defer to a later Asset audit cycle.
- **Consolidator report wording vs. on-disk reality:** The consolidator stated `Tool_Asset.Create.cs` was "Emptied (pending manual delete by Ramon — file still on disk at size 0)", but on inspection the file (and its `.meta`) are both already gone. End-state matches the plan, so this is fine — just a stale wording in the consolidator's own response. No code action required.
- **Tool-ID kebab-case check** was done by visual inspection of all 14 `[McpTool(...)]` IDs returned by Grep — all are lowercase, dash-separated, no underscores or spaces.
- **No git operations were performed.** Scope was derived from the consolidator's explicit file lists.
